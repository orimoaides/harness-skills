---
name: eval-calibrate
description: AI評価と人間レビューの突き合わせによる較正。一致/見落とし/誤指摘を帯一致で分類して精度カウンタ（calibration/counters.json）を更新し、基準の追加・絞り込み・観点の昇格降格を提案する。ハーネスが育つのはこのスキルが回るときだけ。
when_to_use: 人間（レビュアー・依頼主）のフィードバックが出たとき。「突き合わせて」「較正して」と言われたとき。評価済み案件に人間の修正が入ったとき。
argument-hint: "[評価JSONパス] [人間FB]"
allowed-tools: Read, Glob, Write, Edit
---

# eval-calibrate — 評価するAIを較正する

評価スキルは最初は間違える（精度はガードレール水準から始まる）。間違いを較正データとして回収し、次の評価を1つ賢くする装置がこのスキル。

## 現在のカウンタ
!`cat calibration/counters.json 2>/dev/null || echo "counters.json未作成（このディレクトリでの初回較正時に作成する）"`

## 手順

### 1. 突き合わせ
評価JSON（calibration/）と人間FBを観点単位で照合し、**帯（1–3/4–7/8–10）一致**で3分類（±1点のズレは追わない — 追うと較正が収束しない）:
- **一致**: 同じ帯 → agree加算
- **見落とし**: 人間は問題視、AIはpass帯 or 観点なし → miss加算
- **誤指摘**: AIは低帯、人間は問題なし → false_alarm加算
class Cの**影スコア**も同格に帯一致を記録。未確認テーブルにあった観点への指摘は「未確認由来」として別カウント（見なかった＝手順の問題と、見て外した＝基準の問題は直す場所が違う）。

### 2. カウンタ更新とルール発動
`calibration/counters.json` を更新する。スキーマ:
```json
{ "観点ID": { "agree": 0, "miss": 0, "false_alarm": 0, "unverified_hit": 0, "since": "YYYY-MM-DD", "class": "A|B|C" } }
```
しきい値で提案:
- **miss 2回** → 基準文の追加 or 新観点の起票
- **false_alarm 2回** → 基準文を絞る
- **どの観点にも該当しない人間指摘** → 新観点の起票
- **C観点の帯一致が安定して高い** → B昇格（配点対象化）を提案
- **B観点の誤指摘が減らない** → C降格を提案

### 3. 承認ゲート
**提案止まり。基準の書き換え・昇格降格の実行は人間の承認後**（AIが自分の採点基準を自分で書き換えると、較正が人間ではなく自分の過去に閉じる）。

### 4. 記録
`calibration/YYYY-MM-DD_<案件ID>_calibration.json` に保存: 3分類の明細・カウンタ現在値・発動した提案・人間の裁決。
点数は高いのに人間の総合評価が低い等のズレは、観点でなく**配点プロファイルの修正候補**として別記。

## Gotchas
- 人間FBの「なんか違う」をそのまま観点化しない。どの観点の話か1問だけ確認してから分類。
- 討論モデルの乖離点を優先的に突き合わせる（人間の軍配が専門家の基準文を直す最良データ）。
- 基準を絞った直後の2〜3件は見落とし側の増加を監視（振り子の打ちすぎ防止）。
- カウンタはリセットしない。基準を直したら `since` を更新してそこから再カウント。
